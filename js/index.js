;(function() {
  // function Popup() {

  // }

  // function SlideBlock(imageURL) {
  //   this.imageURL = imageURL;
  //   this.comment = '';
  // }

  // var inputPopup, slideBlockPopup;

  // inputPopup = new Popup();
  // slideBlockPopup = new Popup();

  function parseImageURLs(value) {
    JSON.parse(str)
  }

  parseImageURLs();
}());